
/**
 * An enum that represents the types of data validators that we can have.
 */
export enum VALIDATOR_TYPE {

    TEXT_LENGTH,

    TEXT_CONTAINS,

    EMAIL,

    ARRAY_CONTAINS,

    ARRAY_SIZE,
}